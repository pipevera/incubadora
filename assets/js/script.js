/* $(function() {
  $("a").click(function(event){
    if (this.hash !== "") {
      event.preventDefault();
      var hashtag = this.hash;

      $("html, body").animate({
        scrollTop: 
      }, 500, function(){
        window.location.hash = hashtag;
      });
    }
  })
}) */